/** @type {import('next').NextConfig} */
const nextConfig = {
    eslint: {
      // If you want to ignore ESLint errors during the build
      ignoreDuringBuilds: true,
    },
  };
  
  export default nextConfig;
  